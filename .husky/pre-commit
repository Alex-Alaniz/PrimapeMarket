
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged to lint only the staged files
# Using a more robust way to run lint-staged that's compatible with Replit
set +e
npx lint-staged
exit 0
